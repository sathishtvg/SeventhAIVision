{{- if .Values.redis.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "seventh-ai-vision.fullname" . }}-redis
  labels:
    {{- include "seventh-ai-vision.labels" . | nindent 4 }}
    app.kubernetes.io/component: redis
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    {{- include "seventh-ai-vision.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: redis
{{- end }}
