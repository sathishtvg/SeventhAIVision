# Frontend — Phase 2

Web dashboard: React + TypeScript + Material UI, glassmorphism theme (locked decision, see plan §Context).

Not built in Phase 1. Phase 1 is backend-only — verify the API via Swagger at `http://localhost:8000/docs` and the WebSocket at `ws://localhost:8000/ws/live`.
